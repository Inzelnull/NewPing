//! ==============================================================================
//! Pingツール (NewPing) - メインバイナリエントリポイント
//! ==============================================================================
//! 
//! 本ファイルはTauriデスクトップアプリケーションの実行ファイルエントリポイントです。
//! Windowsのリリースビルド時にはコンソールウィンドウの非表示設定（windows_subsystem）を行い、
//! 共通バックエンドライブラリ `app_lib::run()` を起動します。

// リリースビルド時にWindowsの余分なコンソールウィンドウの立ち上がりを防止する属性設定
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

/// アプリケーションのエントリポイント
/// Tauriの初期化・ハンドラー登録・イベントループ実行を行う `app_lib::run()` を呼び出します。
fn main() {
    app_lib::run();
}

